# orchestration-service 本地开发配置。生产环境优先用同名环境变量覆盖，
# 尤其是 model_provider_enc_key——这里的值只是本地开发用的固定密钥，
# 生产环境必须换成随机生成、妥善保管的密钥。
postgres_dsn: "postgres://agentmesh:agentmesh@127.0.0.1:5432/agentmesh?sslmode=disable"
redis_addr: "127.0.0.1:6379"
grpc_addr: ":9090"
admin_http_addr: ":8082"
model_provider_enc_key: "000102030405060708090a0b0c0d0e0f"
